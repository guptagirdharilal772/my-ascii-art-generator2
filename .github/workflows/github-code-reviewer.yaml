name: Gemini AI Code Review & Merge Gate

on:
  push:
    branches:
      - main
      - master
      - 'feature/**'
  pull_request:
    types: [ opened, synchronize, reopened, closed ]
    branches:
      #- main
       - '**'

jobs:
  gemini_review:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Python & Install Gemini CLI
      
        run: |
          npm install -g @google/gemini-cli
          gemini --version

      - name: Authenticate Gemini CLI
        env:
         GEMINI_API_KEY : ${{ secrets.GEMINI_API_KEY }}
        run: |
          echo "export GEMINI_API_KEY=${GEMINI_API_KEY}" >> $GITHUB_ENV
          echo "🔐 Gemini CLI authenticated"

      - name: Generate Git Diff
        id: diff
        run: |
          echo "🔍 Generating diff..."
          if [ "${{ github.event_name }}" = "pull_request" ]; then
            git fetch origin ${{ github.event.pull_request.base.ref }}
            git diff origin/${{ github.event.pull_request.base.ref }}...HEAD > diff.txt || true
          else
            git fetch origin master 
            git diff origin/master...HEAD > diff.txt  || true
          fi
          echo "📄 Diff saved to diff.txt (preview up to 200 lines):"
          head -n 200 diff.txt || echo "(no diff)"

      - name: Check for No Changes and Bypass
        id: bypass
        run: |
          # If diff.txt is empty or only whitespace, skip the review and exit successfully.
          if [ ! -s diff.txt ] || [ "$(wc -c < diff.txt | tr -d '[:space:]')" = "0" ]; then
            echo "No changes detected — skipping Gemini review."
            echo "REVIEW_OUTPUT<<EOF" >> $GITHUB_ENV
            echo "No changes detected — skipping Gemini review." >> $GITHUB_ENV
            echo "EOF" >> $GITHUB_ENV

            # If this is a pull request, post a short comment to indicate bypass.
            if [ "${{ github.event_name }}" = "pull_request" ]; then
              echo "Posting bypass comment to PR..."
              # Use sticky comment action to add/update a comment
              echo "bypass_comment=true" >> $GITHUB_ENV
            fi

            # Exit 0 so the job is considered successful and will not block merge.
            exit 0
          else
            echo "Changes detected, continuing with Gemini review."
            echo "bypass_comment=false" >> $GITHUB_ENV
          fi
      - name: Collect New Files for Review
        id: new_files
        run: |
         echo "📁 Collecting new files..."
         NEW_FILES=""

         # Determine base ref depending on event type
         if [ "${{ github.event_name }}" = "pull_request" ]; then
          BASE_REF="${{ github.event.pull_request.base.ref }}"
          git fetch origin "$BASE_REF"
          FILES=$(git diff --diff-filter=A --name-only origin/"$BASE_REF"...HEAD)
         else
          # For push events, find default branch dynamically
          DEFAULT_BRANCH=$(git remote show origin | grep 'HEAD branch' | cut -d' ' -f5)
          git fetch origin "$DEFAULT_BRANCH"
          FILES=$(git diff --diff-filter=A --name-only origin/"$DEFAULT_BRANCH"...HEAD)
         fi

         for file in $FILES; do
           case "$file" in
            *.py|*.yaml|*.yml|*.json|*.tf|*.sh)
            echo "Including $file"
            NEW_FILES="$NEW_FILES\n\n===== FILE: $file =====\n$(cat $file)"
            ;;
          esac
         done

         echo "NEW_FILES<<EOF" >> $GITHUB_ENV
         echo -e "$NEW_FILES" >> $GITHUB_ENV
         echo "EOF" >> $GITHUB_ENV


      - name: Run Gemini Review
        id: gemini
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        run: |
          echo "🧠 Running Gemini review..."
          DIFF_CONTENT=$(sed -n '1,20000p' diff.txt) || DIFF_CONTENT=""
          NEW_FILES_CONTENT="${{ env.NEW_FILES }}" || NEW_FILES_CONTENT=""
          PROMPT="You are a world-class autonomous code reviewer. Analyze the following Git diff and new files. Identify syntax errors, security vulnerabilities, and configuration mistakes. Provide feedback in Markdown with severity indicators (🔴 Critical, 🟠 High, 🟡 Medium, 🟢 Low). Return concise, actionable suggestions.\n\nGit Diff:\n$DIFF_CONTENT\n\nNew Files:\n$NEW_FILES_CONTENT"

          # Send prompt to Gemini CLI
          gemini -p "$PROMPT" > review_output.txt || echo "⚠️ Gemini CLI failed to produce output"

          echo "REVIEW_OUTPUT<<EOF" >> $GITHUB_ENV
          cat review_output.txt >> $GITHUB_ENV
          echo "EOF" >> $GITHUB_ENV

      - name: Post Review to Pull Request
        if: ${{ github.event_name == 'pull_request' && env.bypass_comment == 'false' }}
        uses: marocchino/sticky-pull-request-comment@v2
        with:
          header: "🤖 Gemini AI Code Review"
          message: |
            ## 🔍 Automated Gemini Review
            ${{ env.REVIEW_OUTPUT }}

      - name: Post Bypass Comment (No Changes)
        if: ${{ github.event_name == 'pull_request' && env.bypass_comment == 'true' }}
        uses: marocchino/sticky-pull-request-comment@v2
        with:
          header: "🤖 Gemini AI Code Review"
          message: |
            No changes were detected in the PR compared to the base branch. The AI review was skipped and the check passed.

      - name: Check for Critical Issues (Merge Gate)
        if: ${{ env.bypass_comment == 'false' }}
        id: gate
        run: |
          echo "🔎 Checking Gemini output for 🔴 Critical issues..."
          echo "${{ env.REVIEW_OUTPUT }}" > review.txt
          if grep -q "🔴" review.txt; then
            echo "❌ Critical issues found. Blocking merge!"
            exit 1
          else
            echo "✅ No critical issues detected."
          fi

      - name: Log Review Summary
        if: always()
        run: |
          echo "===== Gemini AI Review Summary ====="
          echo "${{ env.REVIEW_OUTPUT }}"
